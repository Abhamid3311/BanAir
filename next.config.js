/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['images.unsplash.com', "plus.unsplash.com", "imgd.aeplcdn.com"],
  },
}

module.exports = nextConfig
